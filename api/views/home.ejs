<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>
    <main>
       <section id="feed">
        <h2>Posts from people you follow</h2>
        <% if (posts.length >0){ %>
            <% posts.forEach(post =>{ %>
            <div class="post">
            <div class="title">
                <h3> <%= post.title %></h3><p>- posted by</p>
                <h3> <%= post.user.firstname %></h3>
                  </div>
                 <small>Posted on <%= new Date(post.createdAt).toLocaleDateString() %></small>

          
               <div class="image"><img src="" alt="post image"></div>
                <p><%= post.description %></p>

            </div>

          <%  }) %>

      <%  } else{%>
        <h3> No post available</h3>
    
   <% } %>

       </section>
        
    </main><script>
       
    </script>
</body>
</html>